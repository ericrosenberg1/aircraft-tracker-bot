# Add your OpenSky API credentials (if needed) and other configurations here
OPENSKY_API_USER = 'your_username'
OPENSKY_API_PASS = 'your_password'

# Path to your downloaded OpenSky's aircraft database CSV
AIRCRAFT_DB_PATH = 'aircrafttype.csv'

# Aircraft type configuration
AIRCRAFT_TYPE = '747'  # Change this to the desired aircraft type

# Add your social media API keys and secrets here
SOCIAL_MEDIA_KEYS = {
    'twitter': {
        'api_key': 'your_api_key',
        'api_secret_key': 'your_api_secret_key',
        'access_token': 'your_access_token',
        'access_token_secret': 'your_access_token_secret'
    }
    # Add other platforms similarly
}